/**
 * <strong>⚠ LEGACY MODULE — SLATED FOR REMOVAL.</strong>
 * <p>
 * This package ({@code dev.ikm.komet.app}) is the legacy {@code dev.ikm.komet:application} module.
 * It is <em>not</em> the shipping Komet desktop application. The desktop app that is actually built,
 * launched, and run is <strong>{@code komet-desktop}</strong> ({@code dev.ikm.ike:komet-desktop},
 * package {@code dev.ikm.komet.desktop.*}) — {@code dev.ikm.komet.desktop.App},
 * {@code dev.ikm.komet.desktop.AppMenu}, and {@code dev.ikm.komet.desktop.AppPages}.
 * <p>
 * <strong>Do not add features or fixes in this package</strong> — changes here have no effect on the
 * running application. Make the change in the corresponding {@code komet-desktop} class instead.
 * This module is retained only until its remaining responsibilities are migrated, after which it
 * will be deleted.
 */
package dev.ikm.komet.app;
