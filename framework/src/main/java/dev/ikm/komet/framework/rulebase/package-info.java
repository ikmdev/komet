package dev.ikm.komet.framework.rulebase;
/**
 * Goal for observations is to implement a standard model loosely based on analysis normal form, but not
 * concerned with using analysis normal form for requests at this point.
 * <p>
 * Goal for actions is to separate specification of actions (with a concept) from implementation of actions,
 * separating the specification of rules from a specific implementation of actions.
 */