package dev.ikm.komet.layout.window;

import dev.ikm.komet.layout.KlStateCommands;
import dev.ikm.komet.layout.context.KlContextProvider;

/**
 * Placeholder for now.
 */
public interface KlJournalWindow<T> extends KlStateCommands, KlContextProvider {
    // TODO add implements KlGadget<T> when implementing this class... Don't want to mess with sealed hierarchy now.
}
